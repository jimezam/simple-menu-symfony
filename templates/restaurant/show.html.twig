{% extends 'common/base-admin.html.twig' %}

{% block title %}Restaurant{% endblock %}

{% block body %}
    <h1>Restaurant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ restaurant.name }}</td>
            </tr>
            <tr>
                <th>Slogan</th>
                <td>{{ restaurant.slogan }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ restaurant.slug }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ restaurant.address }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ restaurant.city }}</td>
            </tr>
            <tr>
                <th>Province</th>
                <td>{{ restaurant.province }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ restaurant.country }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ restaurant.phone }}</td>
            </tr>
            <tr>
                <th>Whatsapp</th>
                <td>{{ restaurant.whatsapp }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ restaurant.website }}</td>
            </tr>
            <tr>
                <th>Instagram</th>
                <td>{{ restaurant.instagram }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ restaurant.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ restaurant.longitude }}</td>
            </tr>
            <tr>
                <th>ActiveMenu</th>
                <td>{{ restaurant.activeMenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_restaurant_index') }}">back to list</a>

    <a href="{{ path('app_restaurant_edit', {'id': restaurant.id}) }}">edit</a>

    {{ include('restaurant/_delete_form.html.twig') }}
{% endblock %}
